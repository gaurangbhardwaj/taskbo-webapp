import React from "react";
import styled from "styled-components";

const Container = styled.div`
  display: flex;
  flex: 1;
  flex-direction: row;
  padding: 20px 10px;
`;

const TaskController = () => <Container>TaskController..</Container>;

export default TaskController;
